// Development fallback admin credentials.
// ONLY used when running in development (import.meta.env.DEV === true)
// and the server-side RPC is missing or fails. Do NOT rely on this in production.

export const DEV_ADMIN_CREDENTIALS = {
  email: 'motorenklaro@gmail.com',
  password: 'NavnitMotor',
};
